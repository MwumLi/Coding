第1章，调用list.h中的函数接口，完成链表的查找，插入和删除，求出链表中元素之和，并把这个节点插入到链表中。
 
第3章，编写Linux内核模块，打印系统中所有进程的名字、状态，PID，进程切换次数（last_switch_count 字段）以及进程的类型（policy字段）。

第4章：利用mmap写一个文件拷贝的程序，同时利用read和write写一个文件拷贝的程序，对比一下两者在拷贝小文件和大文件时的效率有何差别（通过花费时间。）

说明：上机前程序已经写好，上机时首先检查代码，上机结束前半个小时开始检查结果。
